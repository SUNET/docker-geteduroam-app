diff --git a/tpl/profile-download.html b/tpl/profile-download.html
index 3422883..affd5f9 100644
--- a/tpl/profile-download.html
+++ b/tpl/profile-download.html
@@ -21,13 +21,14 @@
 {% endif %}
 
 {% if device == 'apple-mobileconfig' %}
-<p>After opening the file on MacOS, install it by going to <strong>System Settings</strong> → <strong>Privacy & Security</strong> → <strong>Profiles</strong>.</p>
+<p>After opening the file on MacOS, install it by opening the <strong>System Settings</strong> app, click <strong>Profile Downloaded</strong> and then double-click the new profile.'</p>
 <p aria-hidden="true">
 	<picture class="screenshot mac">
-	<source srcset="{{ _basePath }}/assets/mac/sonoma-dark.png" media="(prefers-color-scheme: dark)">
-	<img src="{{ _basePath }}/assets/mac/sonoma.png">
+	<source srcset="{{ _basePath }}/assets/mac/sequoia-dark.png" media="(prefers-color-scheme: dark)">
+	<img src="{{ _basePath }}/assets/mac/sequoia.png">
 </picture>
 </p>
+<p>Due to a <strong>bug in MacOS</strong> it sometimes takes multiple attempts to install the profile. If you can't access eduroam after the first try, give it another shot.</p>
 {% endif %}
 
 {% if device == 'google-onc' %}
